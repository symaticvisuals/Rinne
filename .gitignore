# Rust build output
/target

# Runtime blackboard (per-run state, logs, transcripts, artifacts)
.rinne/

# Claude-generated docs (specs, mockups, brainstorming output)
docs/superpowers/

# Local env / editor
.env
.DS_Store
github.txt

Cargo.lock

.env.Local
.gstack/
