//! `rinne-conductor` — prompt assembly, plan parsing, and the backend client.
//!
//! Phase 0 is a skeleton. The OpenAI-compatible backend client, prompt
//! assembly, JSONC-tolerant plan parsing, and backend fallback land in Phase 4
//! (`CONTEXT.md` §7; `PHASE.md` P4).

/// Placeholder so the crate compiles and links during Phase 0.
pub fn placeholder() {}
